package com.example.corts.ui.composeables

/*

@Composable
fun BottomAppBar(currentScreen: SCREEN, onScreenChange: (SCREEN) -> Unit){

    NavigationBar { // using navigation bar for bottom navigation
        NavigationBarItem( // item in the navigation bar
            icon = { // icon
                Icon(
                    painter = painterResource(
                        R.drawable.baseline_settings_24
                    ),
                    contentDescription = "Settings",
                    tint = MaterialTheme.colorScheme.onPrimaryContainer
                )
            },
            label = { Text("Settings") },
            selected = currentScreen == SCREEN.SETTINGS,
            onClick = { onScreenChange(SCREEN.SETTINGS) }
        )
        NavigationBarItem(
            icon = {
                Icon(
                    painter = painterResource(R.drawable.map),
                    contentDescription = "map"
                )
            },
            label = { Text("Map") },
            selected =  currentScreen == SCREEN.MAP,
            onClick = { onScreenChange(SCREEN.MAP) }
        )
        NavigationBarItem(
            icon = {
                Icon(
                    painter = painterResource(R.drawable.baseline_workspace_premium_24),
                    contentDescription = "global"
                )
            },
            label = { Text("Global") },
            selected =  currentScreen == SCREEN.GLOBAL,
            onClick = { onScreenChange(SCREEN.GLOBAL) }
        )
        NavigationBarItem(
            icon = {
                Icon(
                    painter = painterResource(R.drawable.account),
                    contentDescription = "account"
                )
            },
            label = { Text("Account") },
            selected =  currentScreen == SCREEN.ACCOUNT,
            onClick = { onScreenChange(SCREEN.ACCOUNT) }
        )
    }
} */